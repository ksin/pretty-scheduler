{{#if isShowingNewAttendeeCalendar}}
  {{new-attendee-form event=event createAttendee="createAttendee"}}
  <button {{action "toggleCalendars"}} class="event-calendars__view-availabilities">View available dates for attendees</button>
{{else}}
  {{attendees-availabilities event=event}}
  <button {{action "toggleCalendars"}} class="event-calendars__share-dates">HEY share the dates that you're available OK?</button>
{{/if}}
